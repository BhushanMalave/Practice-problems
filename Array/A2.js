let arr = [1,2,3,4,5];

const sort = (arr) => {
    if(arr[0]<arr[1]){
        console.log("Array Sorted in ascending order")
    }else {
        console.log("Array Sorted in ascending order")
}
}

sort(arr);
